# ===================================================================
#                      UYGULAMA YAPILANDIRMASI
# ===================================================================

# === Veri Kaynağı Ayarları (Supabase PostgreSQL) ===
# Bu yapılandırma, Spring'in Render'daki Ortam Değişkenlerini okumasını sağlar.
# Eğer değişkenler yoksa (yerel makinede), ':' sonrasındaki varsayılan değerleri kullanır.
spring.datasource.url=jdbc:postgresql://${DB_HOST:aws-0-eu-central-1.pooler.supabase.com}:${DB_PORT:5432}/${DB_NAME:postgres}
spring.datasource.username=${DB_USER:postgres.hkdzkkrnltbreemboqfm}
spring.datasource.password=${DB_PASSWORD:Jo4l@U8=oTIsUQ\&w}

# === JPA & Hibernate Ayarları (Değişiklik yok) ===
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# === Stripe Ayarları ===
# Bu anahtarlar da Render'daki Ortam Değişkenlerinden okunacak.
stripe.api.publishable.key=${STRIPE_API_PUBLISHABLE_KEY:pk_test_51RM81h4bNLtNNjxctk4OMTO5dCMNdcFGVh1NDY5KtOz6y3nunxMm6STzA1SLKXnZZiQCDNYa1JvYC4UcgfnSUp5300mlJ4VZmG}
stripe.api.secret.key=${STRIPE_API_SECRET_KEY:sk_test_51RM81h4bNLtNNjxcJoZmmmDcJhJNehyIaz8P7QUKA3orFeZtkg7LRUo8MOQlZkT7vV8xhHUdmoTCtvqACS5J4Iup00bcWFhmdL}

# === Güvenlik Ayarları (JWT) ===
# Bu anahtar da Render'daki Ortam Değişkeninden okunacak.
jwt.secret=${JWT_SECRET_KEY:wv4eWvxyjOPMYDX1CwCWFHuDph9bTd0RMIFBu13f04U=}
jwt.expiration=${JWT_EXPIRATION:86400000}

# === Yapay Zeka Servis Entegrasyonu ===
# Bu adres, Render'ın kendi iç ağındaki FastAPI servisini işaret edecek.
fast-api.base-url=${FASTAPI_BASE_URL:http://localhost:8001}


# Hikari Connection Pool Ayarları
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2

# MySQL DATABASE CONNECTION SETTINGS

# spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_db?useSSL=false&serverTimezone=Europe/Istanbul
# spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Istanbul
# spring.datasource.username=root
# spring.datasource.password=123456


# === JPA / Hibernate Ayarları ===
# spring.jpa.hibernate.ddl-auto=update        
# spring.jpa.show-sql=true                    
# spring.jpa.properties.hibernate.format_sql=true

# spring.mvc.converters.preferred-json-mapper=jackson


# MySQL 8 kullanıyorsan Dialect’i açıkça belirtmek iyi olur
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect










# spring.jackson.serialization.write-dates-as-timestamps=false


# DENEME


# # === Veri Kaynağı Ayarları ===
# spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Istanbul
# spring.datasource.username=root
# spring.datasource.password=123456

# # === JPA / Hibernate Ayarları ===
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# # Explicit Hibernate dialect for MySQL 8
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# spring.mvc.converters.preferred-json-mapper=jackson

# === (Opsiyonel) Tarih/Zaman Serileştirme ===
# spring.jackson.serialization.write-dates-as-timestamps=false
 
